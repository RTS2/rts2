\section{Distribuce funkcí}

Ka¾dé pozorovací stanovi¹tì sestává vìt¹inou z~více ovladatelných
èástí - kamer, montá¾e, støechy. Jak jsme nastínily v~kapitole
o~designu, pro ka¾dé zaøízení napí¹eme damon, který ho bude ovládat.

Existují funkce, které je rozumné spravovat centrálnì. Jedná se v¾dy
o~funkce, které by se na v¹ech zaøízením mìli chovat stejnì. Jako
vhodný pøípad mù¾e poslou¾it autentifikace - pøi pou¾ití veøejného
u¾ivatelského jména a~tajného hesla je vhodné zajistit, aby klientu
staèil jedno u¾ivatelské jméno a jedno heslo.

\subsection{Bez centrálního serveru}

Ka¾dé klient udr¾uje spojení se v¹emi zaøízeními, které potøebuje ke
svému provozu - viz obrázek \ref{fig:client-server-direct}. Na první
pohled velmi jednoduché a elegantní øe¹ení.

\dia{client-server-direct}{Klient bez centrálního serveru, s~pøímími spojeními na zaøízení}

\subsection{S~centrálním serverem}

Mezi klienty a zaøízením le¾í centrální server. Pøes nìj probíhá jistý
díl komunikace mezi zaøízením a serverem.

\subsubsection{V¹e pøes centrální server}

V¹echny funkce probíhají pøes centrální server. Centrální server urèí,
jakému zaøízení jsou urèeny, a po¹le mu je.

\dia{client-server-central}{Klient s~centrálnín serverem bez pøímého
spojení}

\subsubsection{Centrální funkce pøes centrální server, lokální funkce}

\dia{client-server-central-direct}{klient s~centrálním serverem s~pøímím
spojením mezi daemony}

Centrální funkce na centrální server, ten distribuuje informace na
zaøízení. 

\subsection{Závìr}

Rozhodl jsem se pro pou¾ití varianty s centrálním serverem. Hlavním
dùvodem jsou výhody pøi implementaci centrálních funkcí.
