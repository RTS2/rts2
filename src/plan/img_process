#!/bin/bash

echo "Processing $1"

# try to get it to ds9..
# cat $1|xpaset ds9 fits

# image processing


if pwd | grep "C0"; then
	#/home/mates/bin/bdark $1
	/home/mates/bin/detect $1

	PGPLOT_DIR=/home/mates/pgplot
	DISPLAY=tcm:0
	/home/mates/bin/past $1 /home/public/past.cfg 2>$1.past
fi
