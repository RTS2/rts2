#$Id$

BIN = planc test

all: $(BIN)

CFLAGS = @CFLAGS@ @LIBPG_CFLAGS@ -I@top_srcdir@/include -DDEBUG
LDFLAGS = @LIBS@

planc: planc.o selector.o ../db/db.o ../writers/fits.o ../utils/devcli.a

test: test.o selector.o ../db/db.o

%.c: %.ec
	@ECPG@ -o $@ $^

clean:
	$(RM) *~ *.o planc test
