all: bacodine.a bacmail

CFLAGS = @CFLAGS@
LDFLAGS = @LDFLAGS@

bacmail: bacmail.o ../../db/db.o
	$(CC) $(LDFLAGS) @LIB_ECPG@ -o $@ $^

bacodine.a: bacodine.o
	$(AR) rvs $@ $^

clean:
	$(RM) *.a *.o bacmail
