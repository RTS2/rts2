# $Id$

BIN = serverd test

all: $(BIN)

CFLAGS = @CFLAGS@ -I@top_srcdir@/include
LDFLAGS = @LDFLAGS@ @LIB_M@ @LIB_NOVA@ @LIB_PTHREAD@

serverd: serverd.o riseset.o ../utils/devser.a

riseset.o:riseset.c
	$(CC) $(CFLAGS) -c -o $@ $^ -lnova

test: riseset.o

clean:
	$(RM) *.o *~ $(BIN)
