# $Id$

BIN = serverd test

all: $(BIN)

LDFLAGS += @LIBS@ -lnova
CFLAGS += -I/usr/local/include -I@top_srcdir@/include

serverd: serverd.o riseset.o ../utils/devser.a

riseset.o:riseset.c
	$(CC) $(CFLAGS) -c -o $@ $^ -lnova

test: riseset.o

clean:
	$(RM) *.o *~
