# $Id$

BIN = serverd test rts2-state

all: $(BIN)

CFLAGS = @CFLAGS@ -I@top_srcdir@/include
LDFLAGS = @LDFLAGS@ @LIB_M@ @LIB_NOVA@ @LIB_PTHREAD@

serverd: serverd.o riseset.o ../utils/devser.a

riseset.o:riseset.c

test: riseset.o test.o ../utils/config.o

rts2-state: riseset.o rts2-state.o ../utils/config.o

clean:
	$(RM) *.o *~ $(BIN)
